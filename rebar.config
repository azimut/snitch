{erl_opts, [debug_info]}.
{deps, []}.

{relx, [{release, {snitch, "0.1.0"}, [banker, clock, pinboard, sasl]},
        {mode, dev}]}.

{profiles, [{prod, [{relx, [{dev_mode    , false},
			    {include_erts, true},
			    {include_src , false},
			    {debug_info  , strip}]}]}]}.
