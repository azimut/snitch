[
 {banker, [{db_config, #{host     => "${DB_HOST}",
                         username => "${DB_USERNAME}",
                         password => "${DB_PASSWORD}",
                         database => "${DB_DATABASE}"}}]},
 {pinboard, [{port, ${PORT}}]}
].
