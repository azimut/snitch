[
 {banker, [{db_config, [{host    , "127.0.0.1"},
                        {username, "autoaim"},
                        {password, "zaq12wsx"},
                        {database, "binance"},
                        {timeout, 2000}, %% socket connection timeout
                        {auto_reconnect, 1}, %% seconds to wait after a disconnection
                        {port, 5432},
                        {pool_size, 2},
                        {pool_type, round_robin}]}]},
 {pinboard, [{port, 8081}]}
].
